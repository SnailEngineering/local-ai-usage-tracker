# Personal data -- collected usage history, archived session transcripts,
# and the rendered dashboard (it embeds per-project cost/token data inline).
data/
dashboard.html

# Secrets
.env

# Local, machine-specific Claude Code settings (personal paths, permissions)
.claude/settings.local.json

# Installed launchd agent (generated by setup.sh from com.aiusage.plist.example)
com.aiusage.plist

# Python
__pycache__/
*.py[cod]
*.egg-info/

# macOS
.DS_Store
